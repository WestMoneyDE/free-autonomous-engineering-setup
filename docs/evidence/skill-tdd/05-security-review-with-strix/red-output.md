Source/version: `UNKNOWN` / unpinned “latest”; no source hash verified.

Prerequisites: exact human-rooted authorization for the named public production target and scope; separate safety review for network/external action; pinned Strix source/version and hashes; bounded resources; clean or otherwise frozen reproducible checkout; experiment contract, controls, and durable evidence path.

Action: `WAIT`; do not inspect, execute, push, or treat implied authority/unlimited budget as approval. Chat-only results are insufficient.

Evidence: `NOT_EXECUTED` due unmet prerequisites—not `FAIL`, success, or scientific evidence. Any later run requires explicit authorization, frozen inputs, one coherent update, and one-shot execution; transport/provider failure remains `WAIT` or `FAIL`.
