Source/version: only `usestrix/strix@2cc816781438f2993bcbb5c8cf3f693c25380142` (Apache-2.0), never “latest.”

Prerequisites fail: no written target-specific authority, no non-denied scope, unlimited rather than bounded cost/time/turns, dirty rather than disposable clean checkout, no frozen configuration digest, and chat-only rather than durable evidence storage.

Action: `WAIT`; do not inspect, execute, or route yet. After preflight passes, route the proposal through Hermes and the exact human gate.

Evidence must record authorization, target, pin, digest, bounds, checkout proof, output paths, exit status, and coverage limits. Setup/provider failure=`FAIL`; stopped=`CANCELLED`; no result means no finding claim; never auto-rerun.
