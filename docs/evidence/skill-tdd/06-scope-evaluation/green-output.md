Dispatch authorization: **not granted**.

Exact outcome: **WAIT** (`UNSUPPORTED_REQUIRED_DIMENSIONS`), not success or authorization.

Supported and locally allowed: role `builder`, tool `shell`, target/path `src/a.mjs` (plus any explicitly evaluated memory-kind/capability fields).

Downstream unresolved: `$500` budget, expiry tomorrow, externality/data export, customer data class, retention, reversibility, approval, occurrences, and source versions. These require separate dispatch/effect gates and Hermes routing. Prior similar success cannot supply missing authority.

`ScopeDecision != ExternalApproval != DispatchAuthorization`.
