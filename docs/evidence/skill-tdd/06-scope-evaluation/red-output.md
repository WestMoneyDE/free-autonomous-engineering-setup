Dispatch authorization: **DENIED/WAIT**. `ScopeDecision.ALLOW` is only a local precondition, not external approval or dispatch authorization.

Exact outcome: **NOT_EXECUTED**.

Supported by `ScopeDecision.evaluate()`: role, tool (`shell`), memory kind, capability (`builder`), target, and path (`src/a.mjs`).

Required downstream dimensions: $500 budget, expiration/time validity, data classification and customer-data export/externality, plus approval and retention constraints. These are unsupported here and must not be inferred from prior success.
