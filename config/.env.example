# Local OmniRoute quickstart only.
# Upstream examples accept any non-empty Authorization value on the local endpoint.
# For remote/shared deployments configure and use a real endpoint key.
OMNIROUTE_API_KEY=dummy-key
